import React, { useState, useEffect }  from "react";
import "../header/style.css";

export default function Header(){

    //const [ dado, setDado ] = useState({ nome : "" });

    //setInterval( () => {
      //  setDado({ nome: window.location.pathname });

      //  console.log(dado);

    //    }, 3000
    //);


    return(
        <header id="main-header">Espaço Salão</header>
    );

} 